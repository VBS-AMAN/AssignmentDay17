@model IEnumerable<Assignment.Models.AMAN_FREIND>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Frd_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Frd_Place)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Frd_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Frd_Place)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Frd_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Frd_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Frd_Id })
        </td>
    </tr>
}

</table>
